{
    "name": "card-game",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "module": "src/main.ts",
    "scripts": {
        "dev": "bun build ./src/index.html --watch --outdir=dist --sourcemap --target=browser --minify",
        "build": "bun build ./src/index.html --outdir=dist --target=browser --minify"
    },
    "dependencies": {
        "@pixi/layout": "^3.2.0",
        "@pixi/ui": "^2.3.2",
        "pixi.js": "8.16.0"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
